<%= form_with(model: category) do |form| %>
  <% if category.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(category.errors.count, "error") %> prohibited this user from being saved:</h2>

      <ul>
        <% category.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :name, style: "display: block" %>
    <%= form.text_field :name %>
  </div>

  <div>
    <%= form.label :icon, style: "display: block" %>
    <%= form.text_field :icon %>
  </div>
  

  <!--div>
    <!%= form.label :deals %>
    <!% @deals.all.each do |deal| %>
      <!%= check_box_tag "category[deal_ids][]", deal.id, @category.deal_ids.include?(deal.id) %>
      <!%= deal.name %>
    <!% end %>
  </div-->


  <div class="mb-3">
    <%= form.collection_select :deal_id, @deal_all, :id, :name, { prompt: "-- Select transaction --" }, { class: "form-select", id: "transaction-selector" } %>
  </div>
    
  <%= form.hidden_field :author_id, value: author.id %>
  
  <div>
    <%= form.submit %>
  </div>
<% end %>
